rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /images/{allPaths=**} {
      // Anyone can read images (public URLs)
      allow read: if true;
      // No direct client writes — uploads go through backend API
      allow write: if false;
    }
  }
}
