services:
  ubuntu:
    build: .
    container_name: ubuntu_vm
    privileged: true
    ports:
      - "2222:22"
      - "8081:8081"
      - "3000:3000"
      - "8443:443"
    volumes:
      - /home/menonu/ws:/home/ubuntu/ws
      - ./storage/gemini-data:/home/ubuntu/.gemini
      - docker-data:/var/lib/docker
      - ./storage/gh-config:/home/ubuntu/.config/gh
      - ./storage/copilot-home:/home/ubuntu/.copilot
      - ./storage/copilot-cache:/home/ubuntu/.cache/copilot
      - ./storage/claude:/home/ubuntu/.claude
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu, compute, video, utility]
    restart: unless-stopped

volumes:
  docker-data:
  gemini-data:
  gh-config:
  copilot-home:
  copilot-cache:
